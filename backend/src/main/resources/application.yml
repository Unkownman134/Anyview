server:
  port: 8080

spring:
  application:
    name: anyview-backend

  datasource:
    url: jdbc:mysql://localhost:3306/anyview?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: ^T.GKiPl({xfLt0M6w6Pj+FL[(E]Pzu&s-3ScHI]Xb1WleLX_D
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

  data:
    redis:
      host: localhost
      port: 6379
      password: 
      database: 0

ai:
  api:
    url: https://api.openai.com/v1/chat/completions
    key: your-openai-api-key

jwt:
  secret: anyview-secret-key-2024-programming-training-platform
  expiration: 86400000

logging:
  level:
    com.anyview: debug
    org.springframework.security: debug

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  show-actuator: false
